import 'package:rtmp_broadcaster/camera.dart';

void main() {
  var controller;
  // Let's print out the methods of the CameraController using reflection or just compile error
  // Actually, I can just grep its source in ~/.pub-cache/
}
